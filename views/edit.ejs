<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Normal Form</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <section class="py-5">
        <div class="container">
            <div class="row shadow rounded-4 p-5 pt-2">
                <div class="d-flex align-items-center justify-content-between">
                    <h2 class="text-center my-5 text-uppercase">Upadate book details</h2>
                    <div>
                        <a href="/" class="btn btn-primary">Home</a>
                        <a href="/add" class="btn btn-primary">Add</a>
                    </div>
                </div>
                <form method="post" action="/updateBook">
                    <input type="text" name="_id" value="<%= singleBook._id %>" hidden>
                    <div class="mb-3">
                        <label for="type" class="form-label">Type :-</label>
                        <input type="text" class="form-control" name="type" id="type" value="<%= singleBook.type %>" placeholder="Enter type">
                    </div>
                    <div class="mb-3">
                        <label for="amount" class="form-label">Amount :-</label>
                        <input type="number" class="form-control" name="amount" id="amount" value="<%= singleBook.amount %>" placeholder="Enter amount">
                    </div>
                    <div class="mb-3">
                        <label for="category" class="form-label">Category :-</label>
                        <select class="form-select" id="category" name="category" value="<%= singleBook.category %>">
                            <option value="-1">Select category</option>
                            <option value="Food">Food</option>
                            <option value="Rent">Rent</option>
                            <option value="Salary">Salary</option>
                            <option value="Other">Other</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label for="date" class="form-label">Date :-</label>
                        <input type="date" class="form-control" value="<%= singleBook.date %>" name="date" id="date">
                    </div>
                    <div class="mb-3">
                        <label for="description" class="form-label">Description :-</label>
                        <input type="text" class="form-control" value="<%= singleBook.description %>" name="description" id="description" placeholder="Enter description">
                    </div>

                    <button type="submit" class="btn btn-primary w-100">Update</button>
                </form>
            </div>
        </div>
    </section>

</body>

</html>